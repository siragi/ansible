upstream demo {
  {% for server in groups.webserver %}
    server {{ server }};
  {% endfor %}

  {% for server in groups['webserver-py3'] %}
    server {{ server }};
  {% endfor %}
}

server {
  listen 80;

  location / {
    proxy_pass http://demo;
  }
}
